# Build artifacts (make build / make install)
/bin/

# Generated command reference (make docs) — generated on demand, not committed
/docs/cli/

# Coverage profile (make cover)
/coverage.out

# Local secrets — never commit
.env
