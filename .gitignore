# Build artifacts (make build / make install)
/bin/

# Generated command reference (make docs) — generated on demand, not committed
/docs/cli/

# Coverage profile (make cover)
/coverage.out

# Release artifacts (goreleaser)
/dist/
# Generated command-reference bundle built during a release (goreleaser before-hook)
/dist-docs/

# Local secrets — never commit
.env
